console.log(document.body.firstChild); // Accessing the first child of the body element
var a = document.body.firstChild; // Assigning the first child of the body to variable a
console.log(a.parentNode); // Logging the parent node of the first child
console.log(a.parentElement); // Logging the parent element of the first child
